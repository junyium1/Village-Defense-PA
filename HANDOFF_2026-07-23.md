# PROMPT DE REPRISE — Village-Defense-PA (2026-07-23)

> Copie-colle ce fichier entier comme premier message à l'assistant qui reprend.

---

## 1. Ton rôle

Tu es un assistant de dev qui travaille sur **Village-Defense-PA**, un jeu de tower-defense
Unity 6 / URP, réalisé en équipe dans le cadre d'un projet d'école (PA 3). Le jeu est couplé
à un **méta-jeu Discord** (un bot + une API backend : mana, inventaire, objets consommables
qui influent sur la partie).

Ton interlocuteur s'appelle **Yanis**. Il parle français, il attend des **réponses denses et
factuelles en français**, sans remplissage. Il travaille seul sur sa branche mais l'équipe
avance en parallèle sur `main`.

---

## 2. Environnement

| Élément | Valeur |
|---|---|
| Dossier projet | `C:\Users\yanis\OneDrive\Bureau\PA 3\Village-Defense-PA` |
| Unity | **6000.3.2f1**, pipeline **URP** |
| OS / shell | Windows 11, PowerShell (+ Git Bash dispo) |
| Remote git | `git@github.com:junyium1/Village-Defense-PA.git` |
| Branche de travail | **`branche-yanis`** |
| Scènes | `Assets/Scenes/MainMenuScene.unity` (build index 0), `Assets/Scenes/GameScene.unity` |

### Outils
- **Unity MCP** (`mcp__unityMCP__*`) : lecture console, hiérarchie, chargement de scène,
  `execute_code` (C# exécuté dans l'éditeur). **C'est le SEUL moyen autorisé de modifier une scène.**
  - `execute_code` en mode `codedom` = **corps de méthode C# 6** : pas de directives `using`
    (il faut écrire `UnityEngine.UI.Image`, `TMPro.TextMeshProUGUI`…), `Object` est ambigu
    (utiliser `UnityEngine.Object`), pas de fonctions locales, pas de tuples.
- **Blender MCP** (`mcp__Blender__*`) : nécessite que Blender **tourne** avec l'addon MCP
  actif (localhost:9876). Actuellement **injoignable**.

---

## 3. Règles absolues (issues de `AGENTS.md`, à lire au démarrage)

1. **Jamais d'édition manuelle du YAML** d'un `.unity` / `.prefab`. Uniquement via MCP.
2. **Ne jamais toucher aux `.meta`** sauf instruction explicite.
3. **Plan avant code** pour toute demande d'architecture : lister étapes + fichiers impactés,
   attendre la validation de Yanis.
4. **Aucune suppression de fichier sans autorisation.** Loguer toute modif majeure dans
   `cleanup_log.md` (Chemin, Ligne, Action).
5. **Zone interdite** : `Assets/Scriptable Objects/DiscordBridge/ApiConfig.asset` (clé API,
   gitignored — ne jamais committer ni logger). Idem `Library/`, `Temp/`, `Logs/`, `Builds/`.
6. **MVVM strict côté Discord** : les vues (`DiscordBridge.UI`) ne référencent JAMAIS
   `DiscordBridge.Networking` ni `DiscordBridge.DTOs`. Elles écoutent les ScriptableObjects
   de `DiscordBridge.Data` via des events `Action`.
7. **Async Unity 6** : `Awaitable`, pas `Task`. Pas d'`async void` sauf handler d'event UI.
8. **Rendu URP** : alpha **1.0 strict** sur les fonds opaques (0.985 fuit en espace linéaire).
   Coroutines d'anim UI : `deltaTime` plafonné à **0.05 s**.
9. Le fichier `AGENTS.md` demande de finir chaque réponse par `O_O` — **cette règle vise les
   agents OpenCode**, pas toi. Ignore-la sauf si Yanis la redemande.

### Règle git (décision de Yanis du 2026-07-23 — critique)

- Yanis travaille **sur `branche-yanis`** et **absorbe `main`** (`git merge origin/main`).
- **Ne JAMAIS merger `branche-yanis` dans `main`** sans confirmation explicite. Un merge naïf
  a déjà été tenté : **60 conflits** sur `GameScene.unity`, il détruisait soit le travail
  map/grille de Yanis, soit la boutique de l'équipe. Il a été annulé (`git merge --abort`),
  `main` n'a jamais été touché.
- **Git LFS** : `.gitattributes` (`*.fbx filter=lfs`) n'existe **que sur `branche-yanis`**.
  `main` n'en a pas. Si un jour son travail part vers `main`, **retirer LFS d'abord**, sinon
  les coéquipiers sans git-lfs récupèrent des FBX cassés de 133 octets.
- Résolution de conflit sur une scène : **UnityYAMLMerge**, jamais à la main.
  `E:\Program Files\Nouveau dossier\6000.3.2f1\Editor\Data\Tools\UnityYAMLMerge.exe`
  invoqué en `merge -p <base> <theirs> <ours> <output>`.
- Après chaque merge de `main`, **restaurer les fichiers outillage** que `main` supprime :
  `AGENTS.md`, `TASKS.md`, `.mcp.json`, `.claude_memory.md`, `cleanup_log.md`,
  `opencode.json`, `.claudeignore`, `.claude/settings.json`.

---

## 4. État du dépôt (au 2026-07-23, fin de session)

```
branche : branche-yanis
git status : PROPRE (aucune modif non commitée)
HEAD : 60b6c8c  Merge main dans branche-yanis : boutique, pieges, unites et menus de l'equipe
       617047a  Coherence map/grille (zone fixe, sol herbe, grille invisible hors build) + ecran keybinds
```

- `branche-yanis` est **à jour avec `main`** (`git log branche-yanis..origin/main` = 0) et pushée.
- Branche de secours : **`backup-yanis-avant-merge-main`** (état d'avant le merge). À supprimer
  quand Yanis le dira.
- Quirk pré-existant, **pas une régression** : `GameScene.unity` contient un composant
  `Game.Player` orphelin (son GameObject n'existe pas). Présent aussi dans `main` et dans la
  base du merge. Sans effet en jeu normal (`Player` vient de `MainMenuScene` via
  `DontDestroyOnLoad`). Disparaîtra à la prochaine sauvegarde de la scène depuis l'éditeur.

---

## 5. ⚠️ TÂCHE EN COURS — déco « pancarte » de l'écran de liaison Discord

### La demande de Yanis (verbatim)

> « il faut reprendre l'asset de pancarte dans le dossier `Assets/Art` avec le nom `pencarte`
> pour faire la liaison compte discord, donc tu mets des boutons simples comme ceux déjà
> présents mais par dessus tu mets l'image comme celle des menus des touches, disposés de la
> même manière avec la même taille »

Traduction : reskinner l'écran **« Lier mon compte Discord »** pour qu'il ait exactement le
même look que l'**écran Touches** (`KeybindsScreen`) : une pancarte pixel-art en fond, des
boutons simples posés dessus, même disposition et même taille.

### ⚠️⚠️ ÉTAT RÉEL : LE TRAVAIL A ÉTÉ PERDU — À REFAIRE

La session précédente a construit toute la structure via `execute_code`… **alors qu'Unity
était en Play mode** (`Application.isPlaying == true`). Conséquence :

- Les modifications de scène faites en Play mode sont **annulées automatiquement** à la sortie
  du Play. `scene.isDirty` était `False`, `git status` est propre : **rien n'a été écrit sur
  le disque**.
- **Rien à nettoyer, rien à annuler.** Il faut simplement **tout refaire hors Play mode**.

**RÈGLE À RETENIR :** avant toute modif de scène via MCP, **vérifier `Application.isPlaying`
et refuser d'agir si `true`**. C'est déjà noté comme piège dans `TASKS.md` (T-11).

### Les assets

| Chemin | GUID | Note |
|---|---|---|
| `Assets/Art/pixelsarts/pencarte.png` | `d4a01ed7590b6b94682d93795b8fe48f` | source |
| `Assets/Resources/UI/pencarte.png` | `1d075f90c1cb25d5a53032b1bd086946` | **copie chargée par le code** |

Dimensions : **677 × 369 px**. Chargée par `Resources.Load<Sprite>("UI/pencarte")`.

### Le modèle à copier : `KeybindsScreen`

Fichier : `Assets/Scripts/Menus/KeybindsScreen.cs` (namespace `Menus`). Il construit toute son
UI **par code**. Constantes à réutiliser telles quelles :

```csharp
panelWidth  = 1240f                                  // largeur de la pancarte (réf. 1920x1080)
BoardAnchorMin = new Vector2(0.243f, 0.168f)         // zone de bois utile dans le sprite
BoardAnchorMax = new Vector2(0.757f, 0.727f)         // (le cadre et les chaînes restent vides)
TextColor      = #F5E6C8    KeyColor  = #3A281C
KeyHoverColor  = #5C3F2A    Waiting   = #FFC24D
DimColor       = rgba(0,0,0,0.55)
```

Règle importante reprise de `KeybindsScreen.NewButton` : le teintage d'un `Button` **multiplie**
la couleur de son `Image`. Donc → **Image en blanc**, la vraie couleur va dans le `ColorBlock`.

### La cible : `LinkAccountScreen_Panel`

Emplacement : `MainMenuScene` → `StartMenuCanvas` → `LinkAccountScreen_Panel` (**inactif** au
repos, c'est normal — il est ouvert par le bouton `DiscordHitbox`).

Structure **d'origine** (celle qu'il faut retrouver si besoin — c'est l'état actuel sur disque) :

```
LinkAccountScreen_Panel   [OFF]  RectTransform(stretch) + Image(#131024 opaque) + CanvasGroup
                                 + UIPanelAnimator + LinkAccountScreen + PanelMenuBlocker
├── Title            "Lier mon compte Discord"  fs 28
├── StatusText       ""                         fs 22
├── NotLinkedGroup
│   ├── CodeInput      TMP_InputField, charLimit 4, IntegerNumber, placeholder "Code a 4 chiffres"
│   └── SubmitButton   "Valider"  (Image violet #6B4FBF) + UIButtonFX
├── LinkedGroup
│   ├── LinkedInfoText
│   └── UnlinkButton   "Délier"   (Image rouge #BF3833) + UIButtonFX
└── CloseButton      "Fermer"     (Image #3D3861) + UIButtonFX
```

Le composant `LinkAccountScreen` (`Assets/Scripts/DiscordBridge/UI/LinkAccountScreen.cs`)
référence ces objets par **champs sérialisés** (`codeInput`, `submitButton`, `linkedGroup`…).
Les références pointent sur des **fileID**, pas sur des chemins : **reparenter les objets ne
casse rien**. Ne pas les supprimer/recréer, seulement les déplacer et les restyler.

### La recette exacte à réappliquer (validée, chiffres mesurés)

**Étape 0 — sortir du Play mode.** Vérifier `Application.isPlaying == false`.

**1. Le voile plein écran** — sur `LinkAccountScreen_Panel` lui-même :
`Image.sprite = null`, `color = rgba(0,0,0,0.55)`, `raycastTarget = true`.

**2. La pancarte** — nouvel enfant `Plank` (sibling index 0) :
`Image.sprite = Resources.Load<Sprite>("UI/pencarte")`, `color = white`,
`preserveAspect = true`, `raycastTarget = false`,
ancres/pivot centrés (0.5, 0.5), `anchoredPosition = 0`,
`sizeDelta = (1240, 1240 * 369/677)` = **(1240, 675.86)**.

**3. La planche de bois** — enfant `Board` de `Plank` :
`anchorMin = (0.243, 0.168)`, `anchorMax = (0.757, 0.727)`, offsets à zéro → **637 × 378 px**.
`VerticalLayoutGroup` : spacing 8, padding (0,0,4,4), `childControlWidth/Height = true`,
`childForceExpandWidth/Height = false`, `childAlignment = MiddleCenter`.

**4. Déplacer tout le contenu dans `Board`**, dans cet ordre :
`Title`, `NotLinkedGroup`, `LinkedGroup`, `StatusText`, `CloseButton`.
Chaque enfant reçoit un `LayoutElement` (min = preferred, flexible = 0) :

| Objet | LayoutElement (l × h) | Style |
|---|---|---|
| `Title` | 620 × 46 | TMP « **COMPTE DISCORD** », fs 34, Bold, centre, `#F5E6C8` |
| `NotLinkedGroup` | 620 × 168 | `VerticalLayoutGroup` spacing 12, MiddleCenter, expand false |
| ├ `Hint` *(nouveau)* | 600 × 46 | TMP fs 19, `#D2BC99`, « Tape /lier sur le Discord, puis entre ici le code à 4 chiffres. » |
| ├ `CodeInput` | 300 × 52 | Image blanche ; `ColorBlock` normal `#2A1C12`, highlighted `#3C2A1C` ; texte `#F5E6C8` fs 28 centré ; placeholder `#D2BC99` α 0.55 fs 20 |
| └ `SubmitButton` | 240 × 46 | Image blanche ; `ColorBlock` `#3A281C` / `#5C3F2A` |
| `LinkedGroup` | 620 × 116 | même `VerticalLayoutGroup` |
| ├ `LinkedInfoText` | 600 × 44 | TMP fs 24, `#F5E6C8` |
| └ `UnlinkButton` | 240 × 46 | Image blanche ; `ColorBlock` `#5A2320` / `#7A322C` (rouge bois) |
| `StatusText` | 620 × 34 | TMP fs 21, `#FFC24D` |
| `CloseButton` | 220 × 44 | Image blanche ; `ColorBlock` `#3A281C` / `#5C3F2A` |

Labels des 3 boutons : `#F5E6C8`, Bold, fs 22, centrés, RectTransform étiré sur le parent,
`raycastTarget = false`.

`pressedColor` = hover × 0.8 ; `selectedColor` = normal ; `disabledColor` = normal α 0.5.

**5. `Hint` doit vivre DANS `NotLinkedGroup`** (sibling index 0), pas directement dans `Board` :
`LinkAccountScreen.Refresh()` ne connaît que `notLinkedGroup` / `linkedGroup`. Placé dedans,
le texte d'aide se masque tout seul quand le compte est déjà lié.

**6. Fin** : forcer `LayoutRebuilder.ForceRebuildLayoutImmediate(boardRT)`, **remettre
`LinkAccountScreen_Panel` inactif**, `EditorSceneManager.MarkSceneDirty`, puis sauvegarder la
scène via MCP (`manage_scene` action `save`).

### Vérifications de fin

- Hauteurs cumulées (le `Board` fait 378 px de haut) :
  - état **non lié** : 46 + 168 + 34 + 44 + 3×8 spacing + 8 padding = **324** ✓
  - état **lié** : 46 + 116 + 34 + 44 + 24 + 8 = **272** ✓
  - ⚠️ Dans l'**éditeur** les deux groupes sont actifs en même temps → total 408 > 378, ça
    déborde visuellement. **Ce n'est pas un bug** : `Refresh()` n'en laisse qu'un seul actif en
    Play. Ne pas « corriger » en réduisant les tailles.
- `LinkAccountScreen_Panel` doit finir **inactif**. S'il reste actif, `PanelMenuBlocker.OnEnable`
  désactive `Menu3DInput` → **tout le menu 3D devient incliquable**.
- `read_console` : 0 nouvelle erreur.
- Loguer la modif dans `cleanup_log.md` et cocher l'avancement dans `TASKS.md` (ticket **T-05**).

### Point à confirmer avec Yanis
Le titre a été changé de « Lier mon compte Discord » → « **COMPTE DISCORD** » (pour coller au
style « TOUCHES » de l'écran Touches). À valider avec lui.

---

## 6. Les tâches suivantes de Yanis

Yanis finit **sa partie** du projet. Ordre initialement convenu : 1 → 4 → 2 → 3. Il a bifurqué
sur la 3 (déco) ; le reste est inchangé.

### ✅ Tâche 1 — Cohérence map / grille — **FAITE** (commit `617047a`)
Système `LevelZone` : niveau auto-portant (grille + Sol + spawn + village + objectif +
cameraBounds en enfants). `LevelZone.Confirm()` re-bake le NavMesh. Zone fixe posée en éditeur
→ `GameManager` la valide automatiquement au démarrage (`zonePlacer == null` →
`LevelZone.Instance.Confirm()`), donc plus de phase de placement runtime. Bordures invisibles
+ feedback de proximité + clamp caméra également en place.

### 🔄 Tâche 3 — Menu inventaire + déco liaison Discord — **EN COURS**
- La partie « liaison Discord » = la section 5 ci-dessus, **à refaire hors Play**.
- Reste ensuite : appliquer **le même traitement pancarte** à `InventoryScreen_Panel`
  (même parent `StartMenuCanvas`, structure : `Title`, `StatusText`, `RefreshButton`,
  `CloseButton`, `ListScroll` → `ListContainer` → `RowTemplate`, `ActiveBuffsText`).
  Attention : la liste défilante est plus haute que la planche de bois (637 × 378) — il faudra
  soit élargir la pancarte, soit réduire la hauteur du `ScrollRect`, soit accepter que la liste
  déborde du cadre. **À trancher avec Yanis avant de coder.**
- Ticket correspondant : **T-05** dans `TASKS.md`.

### ⬜ Tâche 2 — Minimap — **RIEN N'EXISTE ENCORE**
Piste retenue : caméra orthographique en vue de dessus → `RenderTexture` → `RawImage` dans le
HUD de `GameScene`, cadrée sur `LevelZone.WorldSize`. Aucune contrainte externe, faisable
immédiatement.

### ⬜ Tâche 4 — Remplacer les « pions » par des assets Blender — **BLOQUÉE**
Placeholders à remplacer : `Tourelle Placeholder`, `Castle`, `House`, `LittleHouse`, `Portal`,
`Goblin`, `Golem`.
**Bloquant** : nécessite Blender lancé avec l'addon MCP (localhost:9876), actuellement
injoignable. Blender a aussi crashé le 2026-07-23 vers 16:00
(`EXCEPTION_ACCESS_VIOLATION`, Blender 5.2.0 — sans rapport avec Unity).

### ⏸️ Sujet mis de côté par Yanis — freeze de fin de vague
Symptôme rapporté : « je lance le premier niveau et ça se fige, sans rien afficher » →
précisé : **dans l'éditeur, ça gèle, à la fin / après une vague**.

Investigation menée : **ce n'était pas un crash**. `Editor.log` montrait une sortie de Play
mode propre, aucune stack native, et Unity répondait encore au MCP. Le seul fichier de crash
récent venait de **Blender**, pas d'Unity.

**Suspect n°1 non vérifié** — `Assets/Scripts/Game/CombatManager.cs:110` :

```csharp
waveInProgress = false;
if (currentWave >= _levelData.totalWaves)
{
    yield return new WaitUntil(NoEnemyAlive);   // ← boucle pour toujours si un ennemi est bloqué
    ...
    GameManager.Instance.OnGameOver(true);
}
```

`NoEnemyAlive()` ne renvoie `true` que si **aucune** unité de faction `Enemy` n'est vivante. Un
ennemi coincé par le pathfinding (NavMesh) fige la coroutine **silencieusement**, sans erreur
console — ce qui colle exactement à « ça gèle à la fin d'une vague ».

⚠️ **Yanis a explicitement dit « ou oublie »** sur ce sujet. **Ne pas relancer l'enquête sans
son feu vert.**

Note annexe : `CombatManager.cs:154` contient un `print("smt died");` de debug, et
`CombatManager.cs:113` calcule deux fois la même valeur (`CrystalsEarned` puis
`int crystalsEarned`, sur la même ligne) — cosmétique, à nettoyer un jour.

---

## 7. Pièges connus (les vrais, déjà rencontrés)

1. **Play mode + MCP = travail perdu.** Vérifier `Application.isPlaying` avant toute édition de
   scène. C'est ce qui a fait perdre la session du 2026-07-23.
2. **`LinkAccountScreen_Panel` / `InventoryScreen_Panel` laissés actifs** → `PanelMenuBlocker`
   coupe `Menu3DInput` → menu 3D mort.
3. **Teintage des `Button`** : la couleur du `ColorBlock` multiplie celle de l'`Image`. Image
   blanche + couleur dans le `ColorBlock`, sinon les couleurs se mélangent.
4. **`Village-Defense-PA.slnx`** est régénéré tout seul par l'IDE/Unity et bloque les merges
   (« local changes would be overwritten »). C'est un artefact : `git checkout -- Village-Defense-PA.slnx`.
5. **Comparer avec `null` un objet Unity** : ne pas utiliser `??` ni `?.` sur les types dérivés
   de `UnityEngine.Object` (fausse valeur nulle). Utiliser `if (x == null)`.
6. **`Collider.bounds` renvoie (0,0,0)** en mode Édition après un refit (cache PhysX) — la
   donnée fiable est la `size` locale.
7. **TextMeshPro 3D** : la face lisible regarde le **−z**. Textes en miroir → rotation Y = 180°.
8. **`manage_scene` action `load`** : passer `name: "GameScene"` + `path: "Assets/Scenes"`,
   pas `scene_path` (sinon « Either 'name'/'path' or 'buildIndex' must be provided »).

---

## 8. Fichiers à lire en priorité au démarrage

| Fichier | Pourquoi |
|---|---|
| `AGENTS.md` | conventions du projet (source des règles §3) |
| `TASKS.md` | tickets, notamment **T-05** (menus 2D) |
| `.claude_memory.md` | memory bank détaillée : architecture, historique, pièges |
| `cleanup_log.md` | journal des modifications majeures |
| `Assets/Scripts/Menus/KeybindsScreen.cs` | **le modèle visuel à copier** |
| `Assets/Scripts/DiscordBridge/UI/LinkAccountScreen.cs` | la vue à reskinner |
| `Assets/Scripts/Game/CombatManager.cs` | boucle de vagues (freeze suspecté) |
| `Assets/Scripts/Game/GameManager.cs` | états de jeu, auto-confirm de `LevelZone` |

---

## 9. Première action attendue

1. Lire `AGENTS.md`, `TASKS.md`, `.claude_memory.md`.
2. Vérifier que le MCP Unity répond et que **Unity n'est PAS en Play mode**.
3. `git status` — doit être propre sur `branche-yanis`.
4. Demander à Yanis par quoi il veut commencer : **refaire la déco pancarte Discord**
   (section 5, recette prête à appliquer), **la déco de l'inventaire**, ou **la minimap**
   (tâche 2, rien de bloquant).
