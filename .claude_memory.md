# 🧠 MEMORY BANK : LAST MAGICIANS (Frontend Unity)
> **Dernière mise à jour :** 2026-07-19 (session 2 — OpenCode Go) — **PASSAGE EN MODE SOLO** : Claude Code hors de la boucle, OpenCode Go = dev principal. Config MCP `opencode.json` créée (unityMCP stdio/6400, socket testé UP), `AGENTS.md` réécrit (boot : lire memory+TASKS), convention `O_O` adoptée. Merge `main-propre`→`main` **poussé** (`c7c8592`) : divergences remote résolues. **Session 3 (2026-07-19) : import du menu 3D « signpost » + application des couleurs = FAITS ; la suite (assainir transforms → caméra → textes → hover → pirouette) est confiée à OpenCode via les tickets T-06 → T-10.** Détails complets : section « 🪧 MENU 3D SIGNPOST » en bas de ce fichier.

## 🎯 MISSION ET ÉTAT ACTUEL
- **Projet :** Tower Defense hybride en Unity (URP)[cite: 3].
- **Objectif :** Développer et maintenir le client Unity (Frontend C#) qui se synchronise avec un méta-jeu Discord (Backend Python) via une API REST[cite: 3].
- **Règles :** Respect strict de `CLAUDE.md` (Plan avant code, log des suppressions, utilisation du pont MCP)[cite: 1].

## 🔌 PROTOCOLE MCP (RECONNEXION À CHAQUE SESSION)
- **Outil :** Serveur CoplayDev gratuit via `uvx` (`mcpforunityserver==10.1.0`)[cite: 4]. Ne pas utiliser le relais officiel (licence requise)[cite: 4].
- **Transport :** Stdio. Unity Socket Port : 6400.
- **Action requise :** Vérifier que "Window > MCP for Unity" affiche 🟢 "Running" avant d'interagir avec la scène. Si timeout, ne pas deviner la structure, patienter et relancer.

## 🏗️ ARCHITECTURE FRONTEND (Assets/Scripts/DiscordBridge/)
L'architecture C# suit un pattern MVVM strict sans exceptions réseau (`ApiResult`)[cite: 3].
- **Networking :** `DiscordAPIBridge.cs` (MonoBehaviour Singleton)[cite: 3].
- **Data (ScriptableObjects) :** `PlayerProfileData`, `InventoryData`, `ItemDatabase`[cite: 3].
- **Session :** `SessionStore.cs` persiste le `discord_id` via PlayerPrefs[cite: 3].
- **Controllers :** `LinkAccountController`, `ProfileSyncController`, `WaveRewardController`[cite: 3].
- **Règle absolue :** L'UI ne connaît jamais le Networking ni les DTOs, elle écoute uniquement les ScriptableObjects via des events (`Action`)[cite: 3].

## 📡 BLUEPRINT BACKEND (CONTRAT D'INTERFACE REST)
- **Réseau :** Polling HTTP/JSON pur (aucun WebSocket). Mono-process uvicorn. Exposé via Cloudflare Tunnel.
- **Authentification :** Header `X-Api-Key` requis partout. Identifiant unique = `discord_id` (string).
- **Routes :**
  - `POST /api/link-account` : Échange du PIN contre un `discord_id`.
  - `GET /api/player/{discord_id}` : Route de polling principale (Mana, préférences).
  - `GET /api/inventory/{discord_id}` : Récupère les items possédés.
  - `POST /webhook/game-event` : Unity alerte d'une attaque Discord → **Re-poller `/api/player` ensuite.**
  - `POST /api/add-mana` : Récompense de vague. **Route signée HMAC**.
  - `POST /api/consume-item` : Consomme un objet (FIFO).

## ⚠️ PIÈGES RÉSOLUS (ANTI-RÉGRESSION)
- **Newtonsoft.Json :** Requis (installé via `manifest.json`) pour la compilation des DTOs[cite: 3].
- **Signature HMAC (`/api/add-mana`) :** Le timestamp calculé côté C# DOIT avoir une décimale `.0` ajoutée manuellement pour correspondre au formatage float natif de Python côté serveur, sinon la signature échoue[cite: 3].
- **Type du `discord_id` :** Renvoyé en `long` (nombre JSON) sur la route `/link-account`, mais traité comme `string` sur les requêtes `/player` et `/inventory`. Toujours le manipuler en `string` dans Unity[cite: 3].
- **Asynchronisme :** Utilisation exclusive de la nouvelle API Unity 6 `Awaitable` (pas de `Task`) pour garantir le retour sécurisé sur le thread principal[cite: 3].

## ⏳ TO-DO LIST (CE QU'IL MANQUE À FAIRE)

**1. Actions de démarrage : ✅ FAIT (2026-07-18)**
- URL tunnel = `https://api-towerdefence.taqkt.dev` + clé API (64 car.) → injectées dans `ApiConfig.asset`.
- Header confirmé côté C# : `x-api-key` (minuscule ; HTTP insensible à la casse). Smoke test `GET /api/inventory/test` → **HTTP 200** depuis l'Editor.

**2. Assemblage Éditeur :**
- ✅ Asset `ApiConfig` créé → `Assets/Scriptable Objects/DiscordBridge/ApiConfig.asset` (GUID `09c1506c3b1a60249b587e95cae359e6`, **NON versionné / gitignored** car contient le secret).
- ✅ Bootstrap créé : GameObject racine `DiscordBridgeBootstrap` (dans `MainMenuScene`) = `DiscordAPIBridge` (singleton, `config`→`ApiConfig.asset`) + `LinkAccountController` (`profileSyncController` laissé vide pour l'instant).
- ✅ Menu `LinkAccountScreen` **fonctionnel** (gray-box) construit dans `MainMenuScene` sous `StartMenuCanvas` : panneau `LinkAccountScreen_Panel` = Titre + `CodeInput` (TMP_InputField, IntegerNumber, limite 4) + `SubmitButton` (Valider) + `StatusText`. Les 4 refs SerializeField câblées. Testé en Play → rendu OK, 0 erreur.
  - ✅ Panneau **opaque plein écran** (cache le menu derrière), placé en **dernier sibling** de `StartMenuCanvas` (rend au-dessus quand ouvert).
- ✅ **Flux Lier/Délier complet (2026-07-18)** :
  - Bouton **`DiscordAccountButton`** (« Compte Discord », haut du menu, dans `StartMenuCanvas`) + nouveau script `UI/LinkAccountMenuButton.cs` (`openButton` + `linkAccountScreen`) → ouvre l'écran à la demande via `LinkAccountScreen.Open()`.
  - Panneau **caché par défaut** (SetActive false). `LinkAccountScreen` gère 2 états via `Refresh()` : `NotLinkedGroup` (champ code + Valider) / `LinkedGroup` (`LinkedInfoText` "Compte lie" + bouton **Délier**) + bouton commun **Fermer** (`Close()`).
  - `LinkAccountController.Unlink()` → `SessionStore.ClearLinkedAccount()` + event `OnUnlinked` ; la vue re-`Refresh()`.
  - Testé en Play : ouvrir → état lié → Délier → bascule état non-lié (`IsLinked=False`) → Fermer. 0 erreur.
  - 🔸 Détail déco restant : textes sans accents (`Compte lie`, `Delier`, `Compte delie`) pour éviter tout glyphe manquant TMP — à ré-accentuer à la passe déco. Bouton « Compte Discord » à repositionner (chevauche le titre du jeu).
  - ✅ **Liaison validée EN LIVE (2026-07-18)** : PIN réel `3082` → `POST /api/link-account` → 200 `{status:success, discord_id:360486476166856704}`. Chaîne réelle testée : DTO Newtonsoft (`discord_id` long→string) + `SessionStore.SetLinkedAccount` → `IsLinked=True`. Compte créé serveur : `GET /api/player/360486476166856704` → `{mana:0, notif_pref:"mp"}`. **`discord_id` désormais persisté en PlayerPrefs sur cette machine** (clé `DiscordBridge.DiscordId`).
  - ℹ️ Côté serveur, routes temporaires `/debug/generate-pin` (+ 1 autre) ajoutées pour les tests (protégées par clé) — le mainteneur backend doit les retirer avant prod.
- ✅ **Phase A affichage (2026-07-19)** : 6 `ItemDefinition` + `ItemDatabase` créés (`Assets/Scriptable Objects/DiscordBridge/Items/`, VERSIONNÉS). SO runtime `PlayerProfileData.asset` + `InventoryData.asset` créés.
  - `ProfileSyncController` ajouté sur `DiscordBridgeBootstrap` (câblé aux 3 SO) + `LinkAccountController.profileSyncController` câblé → **plus de warning « aucun ProfileSyncController »**, synchro auto au lien.
  - UI menu : `ManaHUD` (haut-droit) ; `InventoryScreen_Panel` (plein écran opaque, caché, dernier sibling : liste `VerticalLayoutGroup` + `RowTemplate`(`InventoryRow`) + `StatusText` + Rafraîchir/Fermer) ; bouton `InventoryButton` (haut-gauche) via `InventoryMenuButton`.
  - Nouveaux scripts : `UI/InventoryRow.cs`, `UI/InventoryScreen.cs`, `UI/InventoryMenuButton.cs`. Compilent (0 erreur CS).
  - 🔸 Déco restant : accents `DisplayName` (stockés sans accents), pas de ScrollRect, positions boutons, icônes vides.
  - ⏳ Vérif Play-mode finale à confirmer (bridge MCP tombé après build, scène déjà sauvegardée sur disque).

**Scène `MainMenuScene` (repères) :** `StartMenuCanvas` (menu jeu, `StartMenuManager`) · `EventSystem` (new Input System / `InputSystemUIInputModule`) · `Player` (progression niveaux) · `MusicManager` · + mes ajouts `DiscordBridgeBootstrap` & `LinkAccountScreen_Panel`.

**3. Code / Logique de jeu : ✅ CODE ÉCRIT (2026-07-19), CÂBLAGE SCÈNE EN ATTENTE**
- ✅ `CombatManager` (fichier gameplay) : events `WaveStarted(int)` / `WaveRewardReady(int wave, int kills)` + `_killsThisWave` + `IsFinalWave`. Crédit d'une vague = au départ de la suivante (ou à la victoire pour la dernière). Zéro référence DiscordBridge dedans.
- ✅ `Controllers/GameplayBridgeHooks.cs` (NOUVEAU) : s'abonne à `CombatManager.Instance` → `WaveRewardController.OnWaveCompleted` (mana HMAC) + `SendGameEventAsync` (alerte Discord ; défaut = dernière vague seulement, `notifyDiscordEveryWave` pour les tests).
- ✅ `Controllers/ConsumeItemController.cs` (NOUVEAU) : `ConsumeAsync(itemId)` → POST /api/consume-item → `ActiveEffectsData.Activate(id, DurationMinutes)` + re-sync inventaire. Events succès/échec.
- ✅ `Data/ActiveEffectsData.cs` (NOUVEAU SO runtime) : `IsActive(id)` / `GetRemainingSeconds(id)` — le gameplay interrogera ça pour appliquer gel/bouclier/boost. ⚠️ L'ASSET n'est pas encore créé (bridge down).
- ✅ `ItemDefinition.DurationMinutes` ajouté ; valeurs injectées À LA MAIN dans les YAML .asset (10/30/5/0) — à vérifier à l'import.
- ✅ `ProfileSyncController` : polling optionnel `pollIntervalSeconds` + verrou `_syncInFlight`. `WaveRewardController` : garde null bridge.
- ✅ UI : `InventoryRow` (bouton Utiliser par callback) + `InventoryScreen` (branche ConsumeItemController) + `LinkAccountScreen` (Mana affiché via PlayerProfileData).
- ✅ « CSS » scripts : `UI/UIPanelAnimator.cs` (fondu+zoom CanvasGroup, unscaled) + `UI/UIButtonFX.cs` (hover/press). Accrochés en optionnel (`panelAnimator`) dans les 2 écrans.
- ✅ **CÂBLAGE SCÈNE FAIT (2026-07-19)** : `ActiveEffectsData.asset` créé (GUID `642835929486c0f43bc7f620dc4cdd24`) ; MainMenu 100% câblée (ConsumeItemController sur bootstrap, polling 30s, bouton Utiliser sur RowTemplate, CanvasGroup+UIPanelAnimator ×2, UIButtonFX partout, LinkAccountScreen.profileData) ; GameScene : GO `DiscordGameplayBridge` = WaveRewardController + GameplayBridgeHooks câblés. Déco : palette violette, boutons arrondis (UISprite Sliced), accents restaurés partout, ScrollRect liste, DiscordAccountButton déplacé (plus de chevauchement titre). **Vérifié en Play : Mana 190 live + inventaire serveur (Skin Fantôme x1).**
- ⚠️ **PIÈGE RENDU (espace linéaire URP)** : alpha 0.985 sur un fond « opaque » fuit visiblement (~20% perçu). Toujours alpha 1.0 pour un fond opaque. Et : coroutines d'anim UI → dt plafonné 0.05s (sinon état figé semi-transparent après hitch/perte focus). Éditeur sans focus = frames gelées → forcer alpha/scale avant toute capture MCP.
- ⚠️ **BRIDGE MCP** : après un redémarrage d'Unity, la fenêtre de setup MCP peut basculer le plugin en mode HTTP (8080) → mes outils (stdio/6400) ne voient plus l'éditeur. Fix : Window > MCP for Unity → transport stdio/6400 → Start.
- ✅ **PHASE C — EFFETS RÉELS (2026-07-19 soir), code + câblage FAITS :**
  - **ÉTAT SERVEUR (fait par le LLM backend, serveur = `/home/taqkt/docker/discord-bridge/main.py` via SSH 192.168.10.160)** : « Prompt 2 » appliqué et testé → table `effets_actifs (discord_id, item_id, expires_at, PK(discord_id,item_id))` ; barème serveur `ITEM_DURATIONS = {shield_10m:600, boost_30m:1800, freeze_5m:300, reinforce:0}` ; `/api/consume-item` pose l'effet (UPSERT `MAX(ancien, now)+durée` = prolonge, jamais raccourcit) et renvoie en plus `duration_seconds` (toujours) + `expires_at` (si durée>0) ; `/api/player/{id}` purge les expirés et renvoie `active_effects:[{item_id, expires_at, remaining_seconds}]` ; **boost_30m ×2 appliqué SERVEUR** (après le cap) dans `/api/add-mana` (+ champ `boosted`), `/ratons` et gains DefenseView (pas BossView). L'achat boutique n'active PAS l'effet (consommation via Unity uniquement).
  - **⏳ SERVEUR EN ATTENTE (« on le fera plus tard ») :** Prompt 1 (resync base + suppression routes `/debug/*` — statut incertain), Prompt 3 (cooldown /ratons 1h, DefenseView timeout 300s + pénalité -25, raids serveur aléatoires, bouclier auto-défense), Prompt 4 (`POST /api/battle-report` → rapport de bataille dans le Fief). Les 4 prompts complets sont dans l'historique de conversation du 2026-07-19.
  - **Client — DTOs** : `PlayerDataResponse.ActiveEffects` (`ActiveEffectDto {item_id, expires_at, remaining_seconds}`) ; `ConsumeItemResponse.DurationSeconds` + `ExpiresAt(double?)`.
  - **Client — Data** : `ActiveEffectsData` += `ActivateForSeconds(id, s)`, `SyncFromServer(list (id, remainingSeconds))` (autoritaire : retire ce que le serveur ne connaît plus, tolérance 2s, notifie sur vrai changement seulement), `GetSnapshot()`. On utilise `remaining_seconds` (relatif) et JAMAIS `expires_at` (décalage d'horloge).
  - **Client — Contrôleurs** : `ProfileSyncController.activeEffects` (nouveau champ câblé) → `ApplyServerEffects` à chaque sync = **les buffs survivent au redémarrage**. `ConsumeItemController` : durée serveur prioritaire, fallback `ItemDefinition.DurationMinutes` ; durée 0 → `PendingItemEffectsStore.Add(itemId)`. `Session/PendingItemEffectsStore.cs` (NOUVEAU, statique, PlayerPrefs `discord_pending_effect_*`) = réserve locale des one-shot (reinforce) déjà décomptés serveur.
  - **Client — Gameplay** : `Game/Health.cs` += `Invulnerable` (TakeDamage ignoré). `Controllers/GameplayEffectsController.cs` (NOUVEAU, sur `DiscordGameplayBridge` en GameScene, scan 0.25s) : `freeze_5m` → NavMeshAgent stop+speed 0 + MeleeAttack/RangedAttack désactivés (restauration à l'expiration, ennemis spawnés pendant le gel attrapés au tick suivant) ; `shield_10m` → `Invulnerable` sur tous les Health non-Enemy ; `reinforce` → consomme la réserve locale et spawn 2 Golems autour de `EnemyObjective.target`. `CombatManager` : victoire = `NoEnemyAlive()` (faction Enemy uniquement, sinon les renforts alliés bloqueraient la fin de partie).
  - **Client — UI** : `InventoryScreen` affiche « Effets actifs : Gel temporel 4:32 · ... » (nouveau TMP `ActiveBuffsText` sous le titre du panneau, MAJ 1s + event), champs `activeEffects`/`itemDatabase`/`activeBuffsText` câblés.
  - **🔧 RÉPARATION CRITIQUE UnitData** : `GolemData.asset` et `GolbinData.asset` avaient `m_Script: {fileID: 0}` (classe `UnitData` déclarée DANS `Unit.cs` → Unity ne peut pas lier). Fix : classe extraite dans `Assets/Scripts/Game/Units/UnitData.cs` (GUID `448a47e84f935c646a6d055dad562eb1`) + `m_Script` des 2 .asset réécrits à la main → les 2 chargent (Ally/Enemy). Prefab `Golem.prefab` : `Unit.data` recâblé sur GolemData + `MeleeAttack` ajouté (gardien statique, pas de NavMeshAgent). `CombatManager.enemyData=GolbinData` vérifié vivant.
  - ⬜ À TESTER EN JEU (non vérifié en Play) : gel/bouclier/renforts en GameScene ; affichage buffs au menu après un consume.
   - ✅ **T-02 FAIT (2026-07-19)** : `BattleReportDTO.cs` (Request/Response, attributs `[JsonProperty]`) + `DiscordAPIBridge.SendBattleReportAsync(BattleReportRequest dto)` → POST `/api/battle-report`, pattern identique à `SendGameEventAsync`. Compile 0 erreur. Prêt pour câblage côté `GameplayBridgeHooks` quand Prompt 4 serveur arrivera.
   - ⬜ Quand le serveur fera Prompt 4 : câbler `SendBattleReportAsync` dans `GameplayBridgeHooks` (remplacer l'alerte « défends-toi » auto par un rapport de fin de partie).

## 🤖 MODE SOLO OPENCODE GO (depuis 2026-07-19, session 2)
- **Claude Code n'est plus dans la boucle.** OpenCode Go (kimi-k3) = dev principal : planification, code, MCP Unity, scènes, backend — tout.
- **Chaîne de démarrage** : `AGENTS.md` (auto-chargé par OpenCode à chaque session) → ordonne de lire `.claude_memory.md` + `TASKS.md` + `git status` en première action. Même mécanisme que CLAUDE.md→memory chez Claude.
- **`opencode.json`** (racine, gitignoré) : configure le serveur MCP `unityMCP` (`uvx mcpforunityserver==10.1.0`, stdio, socket 6400). Chargé au démarrage d'OpenCode uniquement → tout changement = restart.
- **Convention `O_O`** adoptée (fin de chaque réponse).
- L'ancien système duo (délégation `scripts/delegate.ps1`, `.claude/commands/delegate.md`, routage CLAUDE.md) est **conservé sur disque mais inactif** — ne plus l'utiliser.
- T-04 n'est plus « réservé Claude Code » : le MCP étant disponible, les tests Play-mode et le câblage scène sont faisables par OpenCode.

## 📦 ÉTAT GIT (2026-07-19, session 2 — à jour)
- **`origin/main` = `c7c8592`** « Merge branch 'main-propre' » — TOUT est poussé. Contient : les 7 commits Discord Bridge (Phase A/B/C), le fix pause menu du coéquipier (`3c40ec4`), le commit gitignore/screenshots/recovery/scripts (`d2ab58b`).
- **Conflit de merge résolu** sur `.gitignore` : version de `main` conservée (strict sur-ensemble de celle de main-propre : inclut `.opencode/`, `opencode.json`, `.delegate-logs/`, Screenshots, _Recovery, ApiConfig). `GameScene.unity` fusionné auto sans conflit.
- **Secret `ApiConfig.asset` : jamais tracké** ✅ (vérifié — seul `ApiConfig.cs`, la classe, est versionné).
- Branche locale `main-propre` toujours présente (fusionnée, supprimable). Branches `backup/*` et `feature/discord-bridge` locales obsolètes.
- ⚠️ L'historique de main contient toujours les commits `f6e8c4e`/`0cbf749` (fichiers MCP/Claude dans les arbres passés). Purge totale = réécriture d'historique, non faite (et non demandée).

## 🪧 MENU 3D SIGNPOST (session 3, 2026-07-19 — préparé pour OpenCode)
**Objectif :** menu principal 3D façon réf « MAGICIANS UNDER ATTACK! » = panneau bois (signpost) planté dans un décor low-poly (herbe, arbres, nuages), les planches = entrées de menu. Remplace l'écran-titre 2D actuel.

**Assets importés (`Assets/Art/MainMenu/`) :**
- `titlescreen.fbx` ET `menu_signs.fbx` = **DOUBLONS identiques** (~25634 Ko chacun) ; chacun contient TOUTE la scène Blender (décor 745 meshes + panneaux). Redondance à nettoyer plus tard (garder un seul FBX — **demander autorisation avant toute suppression**, logger dans `cleanup_log.md`).
- Import FBX : `materialImportMode:2` (ImportViaMaterialDescription), `materialLocation:1` (External), `useFileScale:1`, `globalScale:1`, `addColliders:0`, `animationType:2` (Generic).
- Matériaux embarqués (noms) : `Scene_-_Root` (17 planches bois), `Iron` (Cube.002/004), `material` (chaînes), `Tree Bark`, `Tree Leaves`, + `Material.001` (herbe/feuillage ×418), `Wood`, `Material.002`, `Lit`, sol/dirt.

**Couleurs — FAIT ✅** (vérifié capture `Assets/Screenshots/mm_colorcheck.png`, gitignoré) :
- Les FBX arrivaient GRIS : **aucune image texture référencée dans le binaire FBX** (les images n'étaient pas branchées sur Base Color côté Blender). Fix appliqué **sans retoucher Blender** — matériaux URP/Lit créés dans `Assets/Art/MainMenu/Materials/` puis remappés sur les 2 FBX via `ModelImporter.AddRemap(...)` + `SaveAndReimport()` :
  - `MM_Wood.mat` (0.54,0.35,0.18) ← `Scene_-_Root`
  - `MM_Iron.mat` (0.55,0.56,0.58 ; metal 0.75) ← `Iron`
  - `MM_Chain.mat` (0.48,0.50,0.53 ; metal 0.85) ← `material`
  - `MM_Bark.mat` (0.29,0.20,0.13) ← `Tree Bark`
  - `MM_Leaves.mat` (0.23,0.48,0.18) ← `Tree Leaves`
- Résultat : troncs bruns, feuillage vert, panneau bois brun, herbe verte. OK.

**⚠️ PROBLÈME TRANSFORMS (à régler AVANT textes/anim = ticket T-06) :**
- Objets importés ont gardé les **coords monde Blender** (~378, 84, 190), **scale minuscule** (signpost ~1 m, planches ~0.14 m), et `sign1_0.006` a **Scale 100 / rotation -90°**. Point de transform ≠ rendu réel (écart ~90 u : ex. `Root_TitleBoard` pos (396.59,5.73,104.42) vs bounds.center (378.94,84.67,198.30)). → regrouper sous un `Signpost_Root` propre, transforms remis droits.

**Les 3 panneaux modélisés (Blender) :**
1. Panneau titre (signpost + planches) → remplace l'écran-titre ; porte le menu principal.
2. Panneau « level select » (pancarte-flèche).
3. Panneau « options » **enchaîné** (chaînes) — le **compte Discord réutilise ce même panneau enchaîné**. Inventaire : pas encore décidé.

**Mécaniques prévues :**
- **Pirouette** : clic planche → le signpost tourne ~180°, contenu swappé à mi-rotation (90°) → sous-menu. Un seul jeu de planches suffit.
- **Hover** : contour blanc + scale ~1.05 + inclinaison ~6° autour du pivot « clou ».
- **Panneau enchaîné (options/discord)** : anim « drop » (tombe avec les chaînes).

**Logique menu DÉJÀ CODÉE** — `Assets/Scripts/Menus/MenuManager.cs`, `namespace Menus`, `class StartMenuManager` : champs `mainMenuPanel` / `optionsMenuPanel` / `levelMenuPanel` ; méthodes `GoToLevelSelect()` (→ `LevelSelectManager.Instance.RefreshButtons()`), `GoToMainMenu()`, `OpenOptions()`, `CloseOptions()`, `WipeSave()` (→ `Player.Instance.ResetProgress()`), `QuitGame()` (`Application.Quit()`), static `OpenLevelSelectOnStart`. **Les clics des planches 3D doivent APPELER ces méthodes — ne pas recoder la navigation.**

**✅ T-06 FAIT (2026-07-19, session 4) — hiérarchie en place :**
- `menu_signs` (renommé **`MENU_3D`**, dépacké) › `Signpost_Root` {`Pole`, `Board_Title`, `Plank_LevelSelect`, `Plank_Options`, `Plank_Wipe`, `Plank_Quit`} · `LevelSelectSign_Root` {`Pole`, `Board`} · `ChainedSign_Root` {`Board`, `ChainL`, `ChainR`} · `Decor` (760 objets) › `BlenderRefs` {`Camera`, `Light` — composants **désactivés**}.
- **⚠️ PIÈGE PRÉFAB FBX (anti-régression)** : un FBX glissé en scène = **instance de prefab modèle** → tout `SetParent`/modif structurelle est **refusé silencieusement** (« resides in a Prefab instance »). **TOUJOURS `PrefabUtility.UnpackPrefabInstance(go, Completely, AutomatedAction)` AVANT de réorganiser** une hiérarchie importée.
- Wrappers = pivots propres (scale 1 / rot 0) pour les anims T-09/T-10 : `Signpost_Root` = centre poteau (pirouette axe Y), `ChainedSign_Root` = centre board (drop), `LevelSelectSign_Root` = centre poteau. Les transforms d'import (scale 100/rot 270) restent **intacts en interne** — ne jamais y toucher (pivots cuits dans les vertices).
- Caméra Blender (`Decor/BlenderRefs/Camera`, wPos ≈ 379.0, 84.6, 199.2, rot (0,182,2)) = référence de cadrage T-07. Panneaux secondaires : LevelSelect ≈ (378.6, 85.0, 200.6) · Chained ≈ (378.2, 84.9, 200.5) · Signpost titre ≈ (378.9, 84.6, 198.3).
- Captures `Assets/Screenshots/t06_avant.png` = `t06_apres.png` (gitignorées) : preuve rendu identique. Meshes « perdus » signalés mais conservés sous Decor : `sign1__0.004/0.005` (loin, ~(593,208,713)), `sign1__0.006` (~(314,-11,164)), `sign2__0.007/0.008/0.009` + `sign3__0.003` (au sol, à l'écart ~(398,0,108)) — suppression éventuelle = demander d'abord.

**✅ T-07 FAIT (2026-07-19, session 4) :**
- **Décision : sous-menus 100% 3D** (l'utilisateur a refusé l'option 2D). Les panels 2D de menu (`mainMenuPanel`, `optionsMenuPanel`, `levelMenuPanel` sous `StartMenuCanvas`) seront retirés du flux à l'étape fonctionnelle.
- Panneaux secondaires réalignés sur le point focal puis **placement final fait par l'utilisateur** (LS/Chained, désactivés) — ne pas écraser. **Cadrage caméra fait par l'utilisateur** (pos (378.975,84.625,198.939), rot (6.8,178.9,0.2), near 0.05) — ne jamais réinitialiser.
- `MM_Dirt.mat` (marron clair) assigné à `Plane.001` directement sur le renderer de scène (méthode retenue pour tout correctif couleur futur : **assignation scène**, pas remap FBX — le remap ne met pas à jour les instances déjà en scène).

**✅ T-08 FAIT (2026-07-19, session 4) :** 7 TMP 3D posés (titre + 4 planches + 2 titres panneaux secondaires). **⚠️ PIÈGES** : (1) `TextMeshPro` 3D rotation identity = face lisible vers **−z** → rotation **Y=180°** obligatoire pour la caméra du menu (qui regarde −z) ; (2) neutraliser le scale d'import par `localScale = 1/parent.lossyScale` puis travailler en unités monde ; (3) auto-sizing TMP ne se calcule pas sur objet **inactif** (titres LS/Options à vérifier à la 1re activation — T-10). Libellé Wipe = « Réinitialiser » (choix utilisateur). Couleur textes crème (0.96,0.92,0.82).

**État disque au handoff (fin session 3) :**
- ⚠️ **L'utilisateur a déplacé le panneau principal À LA MAIN dans `MainMenuScene` puis sauvegardé.** Son placement fait foi visuellement : T-06 doit le **préserver** (assainir les transforms autour, ne pas écraser sa position).
- `git status` : `MainMenuScene.unity` (M, contient le déplacement utilisateur) · `Assets/Art/` (**non tracké** : FBX + `Materials/MM_*.mat` + remaps matériaux écrits dans les `.meta` des FBX — si perdu, les couleurs sont perdues) · `ProjectSettings/URPProjectSettings.asset` (M, auto Unity). → Commit de sécurisation recommandé avant de démarrer T-06.
- La capture couleurs `Assets/Screenshots/mm_colorcheck.png` est gitignorée (normal).

**Conventions décidées :**
- Regroupement scène : vide `MENU_3D` (décor + signpost) ; `Signpost_Root` › `Board_Title`, `Plank_LevelSelect`, `Plank_Options`, `Plank_Wipe`, `Plank_Quit` ; décor sous `MENU_3D/Decor`. L'UI 2D existante (Inventaire / Discord / ManaHUD sous `StartMenuCanvas`) reste **intacte**.
- Scripts menu dans `Assets/Scripts/Menus/`, un script par rôle (`SignPlankHover.cs`, `SignpostRotator.cs`).
- Anim réactives (hover, pirouette, drop) **par code/coroutine**, `deltaTime` plafonné **0.05** ; Animator/Timeline réservé aux boucles d'ambiance (nuages/vent).
- Textes = `TextMeshPro` **3D** enfant de planche (pas un Canvas par planche).
- Réalisation = tickets **T-06 → T-10** (`TASKS.md`), séquencés, plan-avant-code chacun, scène via MCP.

**AVANCEMENT (2026-07-20, Kimi K3 puis Claude/MCP) :**
- **Kimi K3** a écrit la nav (scripts `Assets/Scripts/Menus/` : `SignPlankBase`, `Menu3DInput`, `SignPlankAction`, `SignLevelPlank`, `SignOptionToggle`, `Menu3DController`), la grille 8 niveaux (LevelData réels + couleurs verrouillé/terminé/dispo), le toggle Musique. **Arrêté (crédit épuisé) en plein T-10.**
- **Claude (MCP) a repris** : `SignpostRotator.cs` (pirouette 0→90→−90→0, swap à la tranche, `IsBusy` gèle l'input), `SignPlankHover.cs` (scale 1.05 + tilt 6° autour du centre bounds — pivots importés ~80 u loin de la géométrie, base capturée au 1er hover), `PlankOutline.shader` (inverted-hull, contour **partiel** sur planche plate). `Menu3DController`/`Menu3DInput` réécrits pour router par la pirouette + hover/gating.
- **Scène** : `FlipPivot` sur l'axe poteau ; les 3 panneaux **co-localisés** (boards alignés) + reparentés sous FlipPivot ; **caméra FIXE à X=377.95** (⚠️ NE PAS remettre 378.975 = ancien cadrage sous-panneau → laisse le signpost hors-champ ; le domain-reload recharge la scène du disque, revérifier la caméra après un compile). 15 `SignPlankHover` + `SignpostRotator` câblés. **Vérifié en Play : 3 transitions pirouette OK, hover scale/tilt OK.**
- **Piège clic parasite** : en Play éditeur, la souris physique au-dessus du Game view déclenche des clics via `Menu3DInput` (nav qui dérive). Pour un test déterministe : désactiver `Menu3DInput` et invoquer `Menu3DController.ShowX()` par réflexion. ⚠️ **TOUJOURS le réactiver après** : `Menu3DInput` (sur GO `Menu3D`) est le SEUL composant qui pilote hover ET clic ; désactivé = plancher hover/clic mort. Scène sauvegardée avec `enabled=true` + champ `cam` assigné explicitement à `Main Camera` (le fallback `Camera.main` dans `Awake` suffit au runtime mais on ne dépend plus du tag).
- **Contour → GLOW (2026-07-20, choix utilisateur)** : l'inverted-hull rendait mal sur planche plate → remplacé par un **glow blanc** (`SignPlankHover` éclaircit la planche vers le blanc via `MaterialPropertyBlock` `_BaseColor`/`_EmissionColor`, ×0.6 ; pas de mesh outline, pas de mot-clé shader). `PlankOutline.shader` conservé mais **inutilisé**.
- **Grille Niveaux recadrée** : `LevelSelectSign_Root` scale 0.80→**0.60** + board re-aligné sur le signpost → arrow board + 8 niveaux + « Retour » tiennent dans le cadre.
- **Restant** : doublon FBX toujours présent ; **test souris réel** (hover + clics) à faire par l'utilisateur (la souris ne se pilote pas via MCP).
- **Textures bois (2026-07-20)** : l'utilisateur a exporté les textures Blender dans `Assets/Art/MainMenu/textures/`. Jeu bois signpost = `Image_0.jpg` (base atlas), `Image_2.png` (normal, réimporté en NormalMap) ; jeu `.001` = décor rocheux (non appliqué). `MM_Wood.mat` mis à jour (`_BaseMap`=Image_0, `_BaseColor`=blanc, `_BumpMap`=Image_2, matte smooth 0.2, metal 0). Les UV des planches pointent dans l'atlas → mapping correct (vérifié par rendu). `Image_1` (metallic-rough glTF) non appliqué (packing ≠ masque URP). **Hover/clic réparés** (Menu3DInput réactivé + cam assignée).
- **Prochaine étape demandée** : éclairage **coucher de soleil** (Directional chaude angle bas ~15°, ambiant chaud, brouillard léger, teinte skybox) — attention lisibilité TMP sur bois sombre.