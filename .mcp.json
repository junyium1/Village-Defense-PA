{
  "mcpServers": {
    "unityMCP": {
      "command": "${USERPROFILE}/.local/bin/uvx.exe",
      "args": [
        "--from",
        "mcpforunityserver==10.1.0",
        "mcp-for-unity",
        "--transport",
        "stdio"
      ],
      "type": "stdio"
    }
  }
}
